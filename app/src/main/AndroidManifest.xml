<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.untan.gyazauto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="xyz.untan.gyazauto.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="xyz.untan.gyazauto.AuthActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- open app from browser -->
            <!-- gyazauto://authorize?code=XXX -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="authorize"
                    android:scheme="gyazauto" />
            </intent-filter>
        </activity>

        <activity
            android:name="xyz.untan.gyazauto.SettingsActivity"
            android:label="@string/title_settings" />

        <service android:name="xyz.untan.gyazauto.UploadService" />

        <service android:name="xyz.untan.gyazauto.ScreenshotObserverService" />
    </application>

</manifest>